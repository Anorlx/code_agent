# History

这里保存 `code_agent` 的历史版本快照。

旧的 `week*/day*` 学习过程目录已经整理为按时间顺序排列的版本目录：

- `v01-week1-day1` 到 `v16-week4-day2`

仓库根目录是当前最终版，可直接从根目录运行：

```bash
python main.py
```

注意：历史版本只保留代码和公开说明。运行态数据、API key、本地会话、日志和长期记忆正文不会提交。
